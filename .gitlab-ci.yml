image: node

pages:
  stage: deploy
  script:
    - npm install
    - npm run build
    - npm run export
    - rm -rf public
    - mv out public
  artifacts:
    paths:
      - public # mandatory, other folder won't work
  only:
    - master # or dev, the branch you want to publish
